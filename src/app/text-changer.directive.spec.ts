import { TextChangerDirective } from './text-changer.directive';

describe('TextChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new TextChangerDirective();
    expect(directive).toBeTruthy();
  });
});
